This is a standalone repository for ida developement purpose. It will be merged into harmony repository after fully develop and tested.
It depends on go-raptorq repo. After build the package, to test it, open two terminals with one for each of the commands:
1. ./ida server
2. ./ida client
